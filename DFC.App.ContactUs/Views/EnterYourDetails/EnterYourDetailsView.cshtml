@model EnterYourDetailsViewModel
@{
    Layout = "_LocalLayout";

    if (!string.IsNullOrWhiteSpace(Model.Head?.Title))
    {
        ViewData["Title"] = Model.Head.Title;
    }
}

<partial name="Breadcrumb" model=@Model.Breadcrumb />

<div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
        <form asp-route="/pages/enter-your-details" method="post" class="compui-validation">

            <partial name="EnterYourDetailsForm" model="@Model.EnterYourDetailsBodyViewModel" />

        </form>
    </div>
</div>

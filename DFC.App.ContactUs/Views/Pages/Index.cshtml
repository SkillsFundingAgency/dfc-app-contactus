@model IndexViewModel

@{
    Layout = "_LocalLayout";

    ViewData["Title"] = "Index";
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
        @if (Model.Documents?.Count() > 0)
        {
            <ul>
                @foreach (var document in Model.Documents)
                {
                    if (document.Id == null)
                    {
                        <li>
                            <a href="/@Model.LocalPath/@document.Title">@document.Title</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a href="/@Model.LocalPath/@document.Id/document">@document.Title</a>
                        </li>
                    }
                }
            </ul>
        }
    </div>
</div>
